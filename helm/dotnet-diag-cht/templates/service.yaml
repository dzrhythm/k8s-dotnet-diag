apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-diag-cht.fullname" . }}
  labels:
    {{- include "dotnet-diag-cht.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet-diag-cht.selectorLabels" . | nindent 4 }}
